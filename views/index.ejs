<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }

    h1 {
      text-align: center;
      margin-top: 30px;
      color: #333;
    }

    form {
      max-width: 500px;
      margin: 0 auto;
      background: #fff;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
    }

    label {
      font-weight: bold;
      margin-bottom: 10px;
      display: block;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"],
    input[type="tel"] {
      width: calc(100% - 22px);
      padding: 12px;
      margin: 5px 0 20px;
      border: 1px solid #ccc;
      border-radius: 8px;
      box-sizing: border-box;
    }

    .field-info {
      font-size: 0.9em;
      color: #666;
      margin-bottom: 10px;
      display: block;
    }

    button[type="submit"],
    button[type="button"] {
      background-color: #4caf50;
      color: white;
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      width: 100%;
      transition: background-color 0.3s ease;
    }

    button[type="submit"]:hover,
    button[type="button"]:hover {
      background-color: #45a049;
    }

    button[type="submit"] {
      margin-top: 20px;
    }

    a.button {
      text-decoration: none;
      display: inline-block;
      padding: 12px 24px;
      background-color: #3498db;
      color: white;
      border-radius: 8px;
      transition: background-color 0.3s ease;
    }

    a.button:hover {
      background-color: #2980b9;
    }

    a {
      text-decoration: none;
      display: block;
      margin-top: 20px;
      text-align: center;
    }

    .form-footer {
      overflow: hidden;
    }
  </style>
</head>

<body>
  <h1>Webservice / API Using Nodejs</h1>
  <form id="userForm" action="/save" method="post">
    <label for="firstName">First Name:</label>
    <input type="text" id="firstName" name="firstName" required>
    <span class="field-info">* Enter your first name</span>

    <label for="lastName">Last Name:</label>
    <input type="text" id="lastName" name="lastName" required>
    <span class="field-info">* Enter your last name</span>

    <label for="mobileNumber">Mobile Number:</label>
    <input type="tel" id="mobileNumber" name="mobileNumber" pattern="[0-9]{10}" required>
    <span class="field-info">* 10 digits only</span>

    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>
    <span class="field-info">* Enter a valid email address</span>

    <label for="street">Street:</label>
    <input type="text" id="street" name="street">

    <label for="city">City:</label>
    <input type="text" id="city" name="city">

    <label for="state">State:</label>
    <input type="text" id="state" name="state">

    <label for="country">Country:</label>
    <input type="text" id="country" name="country">

    <label for="loginId">Login ID:</label>
    <input type="text" id="loginId" name="loginId" pattern="[a-zA-Z0-9]{8}" required>
    <span class="field-info">* 8 characters, alphanumeric only</span>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required
      pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[\W_]).{6,}">
    <span class="field-info">* Minimum 6 characters, at least one uppercase, one lowercase, one digit, and one special
      character</span>

    <div class="form-footer">
      <button type="submit">Save</button>
    </div>
  </form>
  <script>
    document.getElementById('userForm').addEventListener('submit', function () {
      setTimeout(function () {
        document.getElementById('userForm').reset();
      }, 1000);
    });

    <% if (errorMessage) { %>
      alert("<%= errorMessage %>");
    <% } %>
  </script>

</body>

</html>